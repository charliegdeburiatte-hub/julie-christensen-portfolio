// FUTURE: Stripe — payment component stubbed, not activated
// To activate: install @stripe/stripe-js and @stripe/react-stripe-js,
// add NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY to .env.local,
// then implement checkout session creation here.

export function PaymentButton() {
  return null
}
